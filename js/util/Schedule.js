/**
 * キャラクタが画面に登場するまでの待機フレームを定義する
 */
class Schedule {
    /**
     * 初期化
     */
    constructor(standByFrm, character) {
        this.standByFrm = standByFrm;
        this.character = character;
    }
}